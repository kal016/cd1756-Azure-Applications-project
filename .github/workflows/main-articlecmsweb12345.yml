- name: Install system dependencies (apt packages)
  if: runner.os == 'Linux'
  run: |
    sudo apt-get update -y
    sudo apt-get install -y build-essential python3-dev
    sudo apt-get install -y default-libmysqlclient-dev libpq-dev unixodbc-dev
    sudo apt-get install -y libssl-dev libffi-dev
